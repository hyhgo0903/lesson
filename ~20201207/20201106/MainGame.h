#pragma once
// 메모리에는 하나만 잡는다
// 사실 잘 모르겠음 구글링
#include <iostream>

using namespace std;

class mainGame
{
public: // 드래그 + 알트엔터로 메뉴호출>>정의만들기
	mainGame(); // 생성자 : 객체 생성과 동시에 자동실행됨
	~mainGame(); // 소멸자 : 객체 종료와 동시에 자동실행됨
	// 함수 선언을 하는것
};

